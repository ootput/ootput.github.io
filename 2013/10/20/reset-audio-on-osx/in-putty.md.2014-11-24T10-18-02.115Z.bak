title: Print to Clipboard in Putty
tags:
  - putty
id: 33
categories:
  - Software
date: 2010-06-26 17:47:39
---

![](http://ootput.files.wordpress.com/2010/06/putty.png?w=76 "putty")

From Diomidis Spinellis - Puttyclip, Putty can be patched to use the clipboard as a printer device:
> A tiny shell script, winclip, can utilize this capability to allow remote (e.g. Unix) programs to pipe their output to the local Windows clipboard, or to copy remote files to the Windows clipboard.

While `left-mouse-button`-_select_, and `middle-mouse-button`-_paste_ (or `shift`-`insert`) interact with Windows' clipboard in much the same way, this patch allows you to pipe remote command output directly to the local clipboard.

While the original patch was authored for an outdated version of Putty, there has since been another to address newer versions.

You can easily patch and build Putty from [source](http://the.earth.li/~sgtatham/putty/latest/putty-src.zip) - Putty's build requirements  in Windows (as detailed in the `[README](http://svn.tartarus.org/sgt/putty/README?revision=8609&amp;view=markup)` file) are minimal.

The patch can be viewed [here](http://gist.github.com/454069#file_putty_0.60_clip.patch), and can be downloaded from [here](http://ericmason.net/putty/putty-0.60-clip.patch).